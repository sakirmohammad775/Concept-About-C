#include <stdio.h>
#include <string.h>

int main() {
    int T; 
    scanf("%d", &T);
    
    while (T--) {
        char S[101];
        scanf("%s", S);
        
        int len = strlen(S); 
        
      
    }
    
    return 0;
}
